apiVersion: v1
kind: Service
metadata:
  name: zeus-monitor
  namespace: zeus
spec:
  type: LoadBalancer
  selector:
    app: zeus-monitor
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
